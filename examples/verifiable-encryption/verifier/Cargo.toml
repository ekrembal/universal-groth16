[package]
name = "verenc-verifier"
version = "0.1.0"
edition = "2021"

[dependencies]
verenc-common = { path = "../common" }
verenc-bridge = { path = "../bridge" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
hex = "0.4"
sha2 = "0.10"
